import { PhoneNoValidatorDirective } from './phone-no-validator.directive';

describe('PhoneNoValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new PhoneNoValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
